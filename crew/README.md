# crew/

Agent definitions by role (planner, implementer, reviewer, tester, researcher, debugger), the ops-only subagent preamble, and playbooks (build-feature: recon → spec → implement → review → fix; triage; release).

# $XDG_CONFIG_HOME/openjawz/identity.env — written by `openjawz onboard`. Mode 0600.
# Every {{KEY}} in ops/templates/*.template is one of these keys. No secrets live here.
AGENT_NAME={{AGENT_NAME}}
USER_NAME={{USER_NAME}}
USER_DOES={{USER_DOES}}
VOICE={{VOICE}}
PUSHBACK={{PUSHBACK}}
NEVER={{NEVER}}
PROFILE={{PROFILE}}
MEMORY_MODE={{MEMORY_MODE}}
BUILD_RULE={{BUILD_RULE}}
OPENJAWZ_HOME={{OPENJAWZ_HOME}}
MODEL={{MODEL}}

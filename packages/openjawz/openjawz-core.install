post_install() {
  echo "==> Run 'openjawz install' as your user to finish setup."
}
post_upgrade() {
  # the migrations hook (95-openjawz-migrations.hook) already announces new migrations; nothing to repeat here
  :
}

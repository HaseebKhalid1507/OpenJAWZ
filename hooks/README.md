# hooks/

Bridges that turn OS events into `synaps send` into the ambient session: `desktop/` (compositor IPC), `fs/` (inotify), `notify/` (in + out), `chronos/` (time), `net/`. `ambient-session.md` is the always-on session's prompt.

# ops/

The discipline: the `SOUL.md` / `OP.md` / `AGENTS.md` / `system.md` / `identity.env` templates the onboarding form fills
(`templates/`), the 8-line ops-only `subagent-preamble.md`, `sacred-files.md`, five discipline skills (`skills/`:
boot-briefing, checkpoint, handoff, shutdown-checklist, sacred-files), the general tools (`tools/`: toolstats, tasks,
sessions, context, transcript + `lib/`), `plugins.list`, and `lib/` (how verbs find the shared library).

Verbs (in `bin/`): `openjawz onboard | boot | checkpoint | shutdown | crew | brain`. No specific person lives here.
Details: `docs/ops.md`, `docs/identity.md`; user manual: `manual/`.
